<UserControl x:Class="Settings.SensorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Settings"
             xmlns:my="Settings.Data"
             xmlns:s="clr-namespace:Settings.Data"
             mc:Ignorable="d"       
             
              >
    <Grid Width="25" Height="25">
        <Canvas >
            <Ellipse x:Name="col" Fill="WhiteSmoke" Width="25" Height="25"/>
            <Ellipse Width="25" Height="25" Stroke="Red"/>
        </Canvas>
        <TextBlock Text="{Binding Address}"    HorizontalAlignment="Center" VerticalAlignment="Center" d:Text="127" FontStyle="Italic" />
    </Grid>
</UserControl>
